<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/beaconFragment">
    <fragment
        android:id="@+id/beaconFragment"
        android:name="com.example.teletron.BeaconFragment"
        android:label="fragment_beacon"
        tools:layout="@layout/fragment_beacon" >
        <action
            android:id="@+id/action_beaconFragment_to_beaconSettingActivity"
            app:destination="@id/beaconSettingActivity" />
    </fragment>
    <activity
        android:id="@+id/beaconSettingActivity"
        android:name="com.example.teletron.BeaconSettingActivity"
        android:label="activity_beacon_setting"
        tools:layout="@layout/activity_beacon_setting"/>
</navigation>